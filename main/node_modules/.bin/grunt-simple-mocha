#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-simple-mocha/bin/grunt-simple-mocha" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-simple-mocha/bin/grunt-simple-mocha" "$@"
  ret=$?
fi
exit $ret
