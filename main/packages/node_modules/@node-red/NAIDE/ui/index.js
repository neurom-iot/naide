var runtime = require('@node-red/runtime');

function init() {
    runtime.httpAdmin.get("/naide/style/*", function(req, res) {
        var options = {
            root: __dirname + '/',
            dotfiles: 'deny'
        };
        res.sendFile(req.params[0], options);
    });
}

module.exports = {
    init: init
};