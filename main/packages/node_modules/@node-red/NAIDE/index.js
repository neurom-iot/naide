var ui = require('./ui');
var terminal = require('./terminal')
var registry = require('./registry')

function init() {
    ui.init();
}

module.exports = {
    ui : ui,
    init: init,
    registry: registry
};