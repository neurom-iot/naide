/**
 * @author K9714
 * @date 20. 11. 18
 */
var api = require("./api");
var installer = require('./installer');

function init() {
    api.init();
    installer.init();
}

module.exports = {
    init: init,
    api: api,
    installer: installer
}